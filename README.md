# C FUNCTIONS

## Build / Debug

```shell
mkdir build
cd build
cmake ..
cmake --build .
./functions_c
```

## Build a graph

The program generates table.csv
Open the ./docs/table.xlsx and enter path to table.csv
